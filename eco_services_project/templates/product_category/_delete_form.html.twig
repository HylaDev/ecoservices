<form method="post" action="{{ path('app_product_category_delete', {'id': product_category.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir continuer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_category.id) }}">
    <button class="btn"><i class="fa fa-trash mx-2 text-danger"></i></button>
</form>
