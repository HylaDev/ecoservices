{% extends 'admin_base.html.twig' %}

{% block title %}Détails{% endblock %}

{% block content %}
    <div class="content">
        <div class="container mt-5">
            <h1>Status</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ qr_status.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ qr_status.name }}</td>
                    </tr>
                </tbody>
            </table>

            <a href="{{ path('app_qr_status_index') }}">back to list</a>

            <a href="{{ path('app_qr_status_edit', {'id': qr_status.id}) }}">edit</a>

            {{ include('qr_status/_delete_form.html.twig') }}

        </div>
    </div>
{% endblock %}
