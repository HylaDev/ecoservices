{% extends 'base.html.twig' %}

{% block title %}QuoteRequest{% endblock %}

{% block body %}
    <h1>QuoteRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quote_request.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ quote_request.date ? quote_request.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ quote_request.total }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ quote_request.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quote_request_index') }}">back to list</a>

    <a href="{{ path('app_quote_request_edit', {'id': quote_request.id}) }}">edit</a>

    {{ include('quote_request/_delete_form.html.twig') }}
{% endblock %}
